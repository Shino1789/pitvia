# ==================================================
# Spring Boot 開発環境設定
# ==================================================
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

app:
  # Security設定
  security:
    swagger-enabled: true
