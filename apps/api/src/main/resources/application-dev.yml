# ==================================================
# Spring Boot 開発環境設定
# ==================================================
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

# CORS設定
app:
  cors:
    allowed-origins:
      - http://localhost:3000
