// ==================================================
// PostCSS設定(Tailwind CSSの変換処理を定義)
// ==================================================
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
